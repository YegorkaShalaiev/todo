import { expect } from 'chai';

describe('Auth: Login', function () {
    it('should do something', function () {
        expect(1).to.be.a('number');
    });

    it('should do something wrong', function () {
        expect(null).to.be.a('string');
    });
});